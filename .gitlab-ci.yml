image: node:latest

cache:
  paths:
    - client/node_modules/

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - cd client
    - npm ci
    - npm run build -- --base-href=/mspr-billeterie/
  artifacts:
    paths:
      - client/dist/MSPR/browser

pages:
  stage: deploy
  dependencies:
    - build
  script:
    - mkdir -p public
    - cp -r client/dist/MSPR/browser/* public/
    - cp client/dist/MSPR/browser/index.html public/404.html
  artifacts:
    paths:
      - public
  only:
    - develop