image: node:latest

cache:
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - npm install
    - npm run build -- --prod --base-href="./"
    - mkdir -p public
    - cp -r dist/*/. public/
  artifacts:
    paths:
      - public
  only:
    - master